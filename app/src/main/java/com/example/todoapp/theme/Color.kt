package com.example.todoapp.theme

import androidx.compose.ui.graphics.Color

// Premium Minimalist Dark Theme (Obsidian & Teal)
val ObsidianBg = Color(0xFF0A0E17)       // Deep space obsidian background
val DarkSurface = Color(0xFF151C2C)      // Slate card surface
val DarkSurfaceElevated = Color(0xFF1E2638) // Elevated card surface
val DarkOutline = Color(0xFF263047)       // Outline border

val AccentTeal = Color(0xFF06B6D4)        // Vibrant Neon Teal (primary accent)
val AccentTealGlow = Color(0xFF22D3EE)    // Lighter Teal
val AccentRose = Color(0xFFFDA4AF)        // Soft rose red for delete/alert

val TextMainDark = Color(0xFFF8FAFC)      // Soft white text
val TextSecDark = Color(0xFF94A3B8)       // Cool gray text
val TextMutedDark = Color(0xFF64748B)     // Muted gray text

// Premium Minimalist Light Theme (Snow & Indigo)
val SnowBg = Color(0xFFF8FAFC)           // Pure Snow Off-White
val LightSurface = Color(0xFFFFFFFF)     // Clean white surface
val LightSurfaceElevated = Color(0xFFF1F5F9) // Slightly grey surface
val LightOutline = Color(0xFFE2E8F0)      // Slate outline

val AccentIndigo = Color(0xFF6366F1)      // Indigo (primary accent)
val AccentIndigoGlow = Color(0xFF818CF8)  // Lighter Indigo

val TextMainLight = Color(0xFF0F172A)     // Deep slate text
val TextSecLight = Color(0xFF475569)      // Slate gray text
val TextMutedLight = Color(0xFF94A3B8)     // Muted gray text
